import React from 'react';
import classes from './FeedList.module.scss';

const FeedList = (props) => {
  return (
  <section className={classes.FeedList}>
    this is feed list section
  </section>
  )
};

export default FeedList;